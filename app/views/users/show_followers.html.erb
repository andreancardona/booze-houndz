<% provide(:title, @title) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <div class="show_followers_header"><%= @user.name %></div>
    </section>
    <section class="stats">
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <div class="col-md-8">
    <div class="follower_header"><%= @title %></div>
    <div class="followers"
    <% if @users.any? %>
  </div>
      <ul class="users following">
        <ul>
          <% @user.followers.each do |u| %>
            <li><%= link_to u.name, u %></li>
        <% end %>
      </ul>
      </ul>
    <% end %>
  </div>
</div>
