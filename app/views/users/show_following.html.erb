<% provide(:title, @title) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <div class="show_following_header"><%= @user.name %></div>
    </section>
    <section class="stats">
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <div class="col-md-8">
    <div class="following_header"><%= @title %></h3></div>
    <div class="followers"
    <% if @users.any? %>
      <ul class="users follow">
        <ul>
          <% @user.following.each do |u| %>
            <li><%= link_to u.name, u %></li>
        <% end %>
      </ul>
      </ul>
    <% end %>
  </div>
  </div>
</div>
